# Projects directory — its own git repo, decoupled from this protocol repo
# Ignore all projects except the sample project (ships with protocol)
/projects/*
!/projects/sample-project/
!/projects/sample-project/**

# Workspace-level config (user-specific)
cartopian.toml

# OS
.DS_Store
